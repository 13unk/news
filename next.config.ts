import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  // basePath and output: 'export' removed to serve from the root
};

export default nextConfig;
